<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="trafficguideMapper">
	 

	 
	<select id="getTrafficguideById" parameterType="int" resultType="com.op.spot.entity.Trafficguide">
		SELECT
         pho ,busline ,selfdrivingroute
	    
	        FROM s_trafficguide 
	        WHERE 
		        sid = #{id }
	</select>
	 
</mapper>